{
  "name": "danishhawk-commerce",
  "version": "1.0.0",
  "private": true,
  "description": "Danish Hawk — distributed commerce platform. Sell bespoke furniture directly from partner websites.",
  "type": "module",
  "engines": {
    "node": ">=22.5.0"
  },
  "scripts": {
    "start": "node server.js",
    "dev": "node --watch server.js",
    "seed": "node src/seed.js",
    "reset": "node src/seed.js --reset",
    "test": "node --test \"test/*.test.js\""
  },
  "dependencies": {}
}
